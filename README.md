# Rust CLI Calculator

~~~bash
$ cargo build --release # To build the calculator
$ cd target/release/ ./rust-cli-calculator <number> <operation> <number> # Move to target/release folder and execute the order the program

#Example
$ ./rust-cli-calculator 1 + 1
1 + 1 = 2 # Output of the program
~~~
